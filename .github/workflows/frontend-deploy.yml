name: Deploy Frontend

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  pull-requests: read

env:
  AZURE_RESOURCE_GROUP: ${{ vars.AZURE_RESOURCE_GROUP }}
  AZURE_CONTAINER_REGISTRY_NAME: ${{ vars.AZURE_CONTAINER_REGISTRY_NAME }}
  AZURE_FRONTEND_CONTAINER_APP_NAME: ${{ vars.AZURE_FRONTEND_CONTAINER_APP_NAME }}
  NEXT_PUBLIC_API_BASE_URL: ${{ vars.NEXT_PUBLIC_API_BASE_URL_PROD }}
  FRONTEND_IMAGE_NAME: finance-foundation-frontend
  AZURE_OPENAI_ENDPOINT: ${{ vars.AZURE_OPENAI_ENDPOINT }}
  AZURE_OPENAI_API_VERSION: ${{ vars.AZURE_OPENAI_API_VERSION }}
  AZURE_OPENAI_DEPLOY_SUMMARY_DEPLOYMENT: ${{ vars.AZURE_OPENAI_DEPLOY_SUMMARY_DEPLOYMENT }}
  AZURE_OPENAI_API_KEY: ${{ secrets.AZURE_OPENAI_API_KEY }}
  LANGFUSE_ENABLED: ${{ vars.LANGFUSE_ENABLED }}
  LANGFUSE_HOST: ${{ vars.LANGFUSE_HOST }}
  LANGFUSE_ENV: ${{ vars.LANGFUSE_ENV }}
  LANGFUSE_PROMPT_LABEL: ${{ vars.LANGFUSE_PROMPT_LABEL }}
  LANGFUSE_DEPLOY_SUMMARY_PROMPT_NAME: ${{ vars.LANGFUSE_DEPLOY_SUMMARY_PROMPT_NAME }}
  LANGFUSE_PUBLIC_KEY: ${{ secrets.LANGFUSE_PUBLIC_KEY }}
  LANGFUSE_SECRET_KEY: ${{ secrets.LANGFUSE_SECRET_KEY }}

jobs:
  deploy-frontend:
    runs-on: ubuntu-latest
    environment: production
    concurrency:
      group: deploy-frontend-production
      cancel-in-progress: false

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Azure login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Build frontend image and deploy to Azure Container Apps
        env:
          GH_TOKEN: ${{ github.token }}
          IMAGE_TAG_SUFFIX: ${{ github.sha }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: bash scripts/deploy-frontend-manual.sh
